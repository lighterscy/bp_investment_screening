# Templates

Place the Word report template here:

```text
templates/investment_memo.docx
```

The renderer should consume structured `InvestmentMemo` data and fill template placeholders. Keep template styling in Word, not in Python code.

